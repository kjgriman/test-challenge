{
  "name": "virtual-speech-therapy-platform",
  "version": "1.0.0",
  "description": "Plataforma virtual de terapia del habla con juegos interactivos y sincronización en tiempo real",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["speech-therapy", "phaser", "websockets", "react", "node"],
  "author": "Tu Nombre",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
